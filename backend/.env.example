# Optional - without this, the app runs in simulated generation mode
# (clearly labeled in the UI), with real retrieval and evaluation still
# fully functional. With a funded key, generation and LLM-judge
# faithfulness checking both use the real Claude API.
ANTHROPIC_API_KEY=

PORT=5004
